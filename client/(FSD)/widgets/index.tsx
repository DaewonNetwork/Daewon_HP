import React from "react"
import MainContent from "./map/MainContent/ui/MainContent";

const map = {
    ui: {
        MainContent
    }
}


const Widget = () => {
    return (
        <div>Widget</div>
    );
};

Widget.map = map;

export default Widget;