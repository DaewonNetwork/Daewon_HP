import React from "react";
import { PhItemType } from "../types/PhItem.type";

const PhItemShared = ({}: PhItemType) => {
    return (
        <div>PhItemShared</div>
    );
};

export default PhItemShared;