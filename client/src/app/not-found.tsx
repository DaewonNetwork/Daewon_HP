import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
    title: "HP - 404 Not Found",
}

const NotFound = () => {
    return (
        <div>
            404 not found
        </div>
    );
};

export default NotFound;